defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  min_messages: warning
  username: <%= ENV['USER'] %>
  host: localhost

development:
  <<: *defaults
  database: carlad_api_development

test:
  <<: *defaults
  database: carlad_api_test
